<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Chỉnh sửa Product</title>
  <link rel="stylesheet" href="../../../css/editProduct.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

</head>

<body>

  <%- include("partials/headerAdmin") %>
    <main>
      <div class="pt-4">
        <div class="row">
          <div class="col-sm-12">
            <div class="updateProduct-form">
              <form action="/admin/product/update/<%=product[0].id%>" method="POST" enctype="multipart/form-data">
                <h2>CẬP NHẬP THÔNG TIN SẢN PHẨM</h2>
                <div class="form-group">
                  <div class="row">
                    <div class="col">
                      <span>Tên sản phẩm</span>
                      <input type="text" name="name" id="name" value="<%=product[0].name%>" class="form-control"
                        required="required">
                    </div>
                    <div class="col">
                      <span>Số lượng sản phẩm</span>
                      <input type="number"  min="0" name="quantity" id="quantity" value="<%=product[0].quantity%>"
                        class="form-control" required="required">
                    </div>
                  </div>
                </div>
                <div class="form-group">
                  <span>Mô tả</span>
                  <input type="text" name="description" id="description" value="<%=product[0].description%>"
                    class="form-control" required="required">
                </div>
                <div class="form-group">
                  <span>Mã SKU</span>
                  <input type="text" name="sku" id="sku" value="<%=product[0].sku%>" class="form-control"
                    required="required">
                </div>
                <div class="form-group">
                  <span>CPU</span>
                  <input type="text" name="cpu" id="cpu" value="<%=product[0].cpu%>" class="form-control"
                    required="required">
                </div>
                <div class="form-group">
                  <span>RAM</span>
                  <input type="text" name="ram" id="ram" value="<%=product[0].ram%>" class="form-control"
                    required="required">
                </div>
                <div class="form-group">
                  <span>Ổ cứng</span>
                  <input type="text" name="hardDrive" id="hardDrive" value="<%=product[0].hardDrive%>"
                    class="form-control" required="required">
                </div>
                <div class="form-group">
                  <span>Màn hình</span>
                  <input type="text" name="screen" id="screen" value="<%=product[0].screen%>" class="form-control"
                    required="required">
                </div>
                <div class="form-group">
                  <span>Card màn hình</span>
                  <input type="text" name="card" id="card" value="<%=product[0].card%>" class="form-control"
                    required="required">
                </div>
                <div class="form-group">
                  <span>Giá tiền</span>
                  <input type="number"  min="0" name="price" id="price" value="<%=product[0].price%>" class="form-control"
                    required="required">
                </div>
                <div class="form-group">
                  <span>Mã loại</span>
                  <select id="category" name="idCategory" value="<%=product[0].idCategory%>">
                    <% for ( var i=0; i < category.length; i++ ) { var selected=( product[0].idCategory==category[i].id
                      ) ? "selected" : "" ; %>
                      <option value="<%=category[i].id %>" <%=selected %>><%=category[i].name %>
                      </option>
                      <% } %>
                  </select>
                </div>
                <div class="form-group">
                  <span>Mã ưu đãi</span>
                  <select id="promotion" name="idPromotion">

                    <% for ( var i=0; i < promotion.length; i++ ) { var selected=(
                      product[0].idPromotion==promotion[i].id ) ? "selected" : "" ; %>
                      <option value="<%=promotion[i].id %>" <%=selected %>><%=promotion[i].name %>
                      </option>
                      <% } %>
                  </select>
                </div>
                <div class="form-group">
                  <span>Chọn hình ảnh</span>
                  <input type="file" name="thumbnail" accept="" id="thumbnail" value="<%=product[0].thumbnail%>"
                    class="form-control">
                </div>
                <div class="form-group">
                  <button type="submit" class="btn btn-success btn-lg btn-block">Update</button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </main>

</body>

</html>